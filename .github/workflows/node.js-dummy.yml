name: Node.js CI (dummy)

on:
  pull_request:
    branches: [ master ]
    paths-ignore:
      - 'frontend/*'

jobs:
  frontend-build:
    runs-on: ubuntu-20.04
    steps:
    - name: 假的測試
      run: echo "dummy frontend CI (always pass)"

