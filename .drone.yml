---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: greeting
  commands:
  - echo "git pull"
  - cd /carbonbond
  - git stash
  - git pull --rebase
  - git stash pop
  - cargo build --release
  - cd /carbonbond/frontend
  - npx webpack -p
  - cd /carbonbond/
  - cp carbonbond.service /etc/systemd/system/carbonbond.service
  - service restart carbonbond.service

trigger:
  branch:
  - develop
  event:
  - push
