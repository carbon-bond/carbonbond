---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: greeting
  commands:
  - echo hello world
  - cd /home/admin
  - touch qq.txt

trigger:
  branch:
  - develop
  event:
  - push
